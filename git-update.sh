#!/bin/bash
cd ~/AWS-Server-Hub; sudo git config --global --add safe.directory /home/israel-master-key/AWS-Server-Hub; sudo git pull; git pull; sudo git add .; git add .; sudo git commit -m "script"; git commit -m "script"; sudo git push; git push;

